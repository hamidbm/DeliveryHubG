services:
  mongodb:
    image: mongo:latest
    container_name: mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: secretpassword
    networks:
      - aura-network

  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: deliveryhub:1.0.0
    container_name: deliveryhub
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      MONGODB_URI: mongodb://admin:secretpassword@mongodb:27017/deliveryhub?authSource=admin
      API_KEY: ${API_KEY}
    depends_on:
      - mongodb
    networks:
      - aura-network

volumes:
  mongo-data:

networks:
  aura-network:
    driver: bridge
