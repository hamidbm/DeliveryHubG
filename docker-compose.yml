services:
  mongodb:
    image: mongo:latest
    container_name: mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: secretpassword
    networks:
      - delivery-network

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: deliveryhub
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      MONGO_URL: mongodb://admin:secretpassword@mongodb:27017/deliveryhub?authSource=admin
      API_KEY: ${API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY}
      AI_DEFAULT_PROVIDER: OPEN_ROUTER
      ADMIN_BOOTSTRAP_EMAILS: hamid@benmalek.net
    depends_on:
      - mongodb
    networks:
      - delivery-network

volumes:
  mongo-data:

networks:
  delivery-network:
    driver: bridge
