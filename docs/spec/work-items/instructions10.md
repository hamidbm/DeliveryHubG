# Next: Program Coordination UI v1 (Consumes /api/program/intel) ## Goal Create a minimal, high-signal Program Coordination screen that helps leaders answer: - Which bundles are at risk? - What are the top cross-bundle blockers? - Which milestones are most at risk? Use /api/program/intel as the data source. Do not add charts. Use compact tables/lists and drilldown modals like Roadmap. --- ## Part A — New Page Add a page: - `src/app/program/page.tsx` Routing: - should be accessible from the main navigation (where other tabs/modules are) - if nav is centralized, add "Program" tab linking to `/program` --- ## Part B — Data Fetch + Lazy Lists On initial load: - call: `GET /api/program/intel?includeLists=false` - render summary + tables using fields returned For drilldowns: - when user clicks "View top blockers" / "View at-risk bundles" / "View at-risk milestones": - re-fetch with `includeLists=true&limit=10` - cache the `lists` results in state to avoid repeated calls - open modal showing relevant list Optional optimization: - support bundleIds/milestoneIds filters via UI (dropdowns) and pass them to the API. If too much, skip filters in v1 and just show global program intel. --- ## Part C — UI Layout (Minimal) ### C1. Summary strip Show: - bundles count - milestones count - workItems count - blockedDerived - highCriticalRisks - overdueOpen ### C2. At Risk Bundles table Use `bundleRollups[].aggregated` and `bundleRollups[].band`: Columns: - Band - Bundle (name if available else id) - Blocked - High+Critical risks - Overdue - Avg confidence - Avg readiness - Late milestones count Row click: - opens a modal showing that bundle’s milestones (from bundleRollups[].milestones) with their confidence/readiness/capacity/blocked/risk. ### C3. Top Cross-Bundle Blockers Show listCount and a “View” button. Modal list shows: - blocker key/title/status - blocker bundle/milestone - blockedCount - sampleBlocked (click to open item details) ### C4. At Risk Milestones Show from `lists.topAtRiskMilestones` (when loaded) or show listCounts + “View” button. Modal list items should link to: - Roadmap filtered to that milestone if you have a query param approach - or navigate to Work Items Roadmap view and highlight milestone if supported If no filter exists, just navigate to Roadmap view and rely on manual selection. --- ## Part D — Reuse Existing Modal + Item Navigation Reuse the same modal pattern used in `WorkItemsRoadmapView.tsx`. When clicking a work item in a modal: - navigate to `/work-items/[id]` (or the existing details route) - or open side panel if you have that mechanism globally --- ## Part E — Tests (Light) Add a basic UI-free test (optional): - already have API; skip UI tests unless you have a framework. At minimum ensure `npm run build` passes. --- ## Deliverables - /program page rendering summary + at-risk bundles table + drilldowns - uses /api/program/intel includeLists=false by default; includeLists=true on demand - links from navigation - no major redesigns