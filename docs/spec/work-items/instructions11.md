# Next: Bundle Profile Intelligence Strip v1 (Applications → Bundle Profiles) ## Goal Integrate the new milestone/roadmap/program intelligence into the Bundle Profile so leaders can see: - current milestone confidence/readiness for this bundle - cross-bundle blockers affecting this bundle - high/critical risks and overdue work summary - one-click drilldowns to Roadmap and Program views Do not redesign Applications module; add a compact section. --- ## Part A — Bundle-Scoped Program Intel API Usage Reuse existing endpoint: - GET /api/program/intel?bundleIds=<bundleId>&includeLists=false For drilldowns in bundle profile: - fetch includeLists=true on demand. If bundle profile already fetches work-item summaries, do not duplicate heavy queries; use the program intel endpoint for this new strip. --- ## Part B — UI Placement In the bundle profile page (likely under `src/app/applications/bundles/[bundleId]` or similar): Add a new section near the top called: - "Delivery Intelligence" Show: 1. Summary chips (bundle-scoped): - blockedDerived - high+critical risks - overdueOpen - avg confidence - avg readiness - late milestone count 2. Milestones table (bundleRollups[0].milestones): - milestone name - confidence score/band - readiness band - capacity utilization - blockedDerived - high+critical risks - slipDays (if late) 3. Drilldowns (lazy lists): - "View cross-bundle blockers" -> modal list from includeLists=true topCrossBundleBlockers - "View at-risk milestones" -> modal list 4. Navigation links: - "Open Roadmap" (pre-filter to bundle if supported, else navigate to Roadmap and set bundle filter) - "Open Program Coordination" with bundleId query param (add query support if missing) Keep layout compact and consistent with existing UI patterns. --- ## Part C — Add Filtering Support to Program Page (If Missing) If /program page does not support URL query params yet, add: - `?bundleIds=...` and `?milestoneIds=...` Read from search params and pass to /api/program/intel calls. Also add small UI filter controls (optional). If too much, URL params only is fine. --- ## Part D — Tests No UI tests required unless you already have a framework. At minimum: - `npm run build` must pass. - Add a small API test to confirm bundleIds filter returns only that bundleRollup. --- ## Deliverables - Bundle Profile shows Delivery Intelligence strip powered by /api/program/intel - Drilldown modals for cross-bundle blockers and at-risk milestones - Program page supports bundleIds query param so Bundle Profile can deep link - Build passes