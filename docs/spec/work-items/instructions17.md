markdown # Next: Notification Preferences v1 (Policy + Routing + Digest) ## Goal Make notifications operationally usable by adding: - admin-configurable enable/disable by notification type - basic routing rules (who gets what) - user-level opt-in/out (minimal) - optional digest mode (daily) without building a full scheduler Keep it MVP: settings + enforcement + UI + tests. --- ## Part A — Data Model Add collections (or extend existing settings collection): 1) `notification_settings` (global/admin) ```ts { _id: 'global', enabledTypes: Record<string, boolean>, // e.g. { "milestone.status.changed": true, ... } routing: { includeAdmins: boolean, includeBundleOwners: boolean, includeActorOnBlocked: boolean }, digest: { enabled: boolean, cadence: 'DAILY', hourLocal: number // default 9 }, updatedAt, updatedBy } ``` 2) `notification_user_prefs` (per user) ```ts { userId, mutedTypes: string[], // deny-list digestOptIn?: boolean // if true, user receives digest instead of immediate for eligible types } ``` Defaults: - all current types enabled - routing defaults match current behavior (admins + bundle owners + actor on blocked) --- ## Part B — Enforce Settings in Pipeline In `src/services/notifications.ts`: 1. Load global settings once per request (cache in-memory with short TTL is fine). 2. Before creating a notification: - if type disabled globally, skip - if user has muted that type, skip - if digestOptIn enabled and type is digest-eligible, enqueue digest instead of immediate notification: - store in `notification_digest_queue` collection: `{ userId, type, title, body, link, createdAt }` Digest-eligible types (v1): - milestone.status.changed - dependency.crossbundle.created (Keep override/blocked/over-capacity as immediate by default) --- ## Part C — Admin UI: Notification Policy Add Admin page: - Admin → Settings → Notifications (or Admin → Notifications Policy) Features: - toggle each notification type on/off - routing toggles (includeAdmins/includeBundleOwners/includeActorOnBlocked) - digest toggle + hour API: - GET/PUT /api/admin/notification-settings RBAC: Admin/CMO only. --- ## Part D — User UI: Preferences (Minimal) Add Profile section or simple page: - /profile (or existing profile UI) Add: - list of notification types with mute toggles (mutedTypes) - digest opt-in checkbox API: - GET/PUT /api/user/notification-prefs RBAC: self only. --- ## Part E — Digest Delivery (MVP, no background scheduler) Implement endpoint to generate a digest for a user: - POST /api/notifications/digest/send?userId=... Admin-only for now or self-call: - loads queued items for that user from last 24h - creates a single notification: type: `digest.daily` title: "Daily Digest" body: markdown list of grouped items with links - deletes queued items included This lets you manually trigger digest in dev and later wire to a cron/automation. --- ## Part F — Tests Add tests: - when type disabled globally → no notification created - when user muted type → no notification created - digest opt-in queues items instead of immediate - digest send endpoint creates digest notification and clears queue - RBAC for admin settings endpoints Add npm script: test:notification-prefs Include in test:api runner. --- ## Deliverables - global notification settings + admin UI - per-user prefs + UI - enforcement in notification pipeline - digest queue + manual digest send endpoint - tests + docs update