# Next: Digest Automation v1 (Cron-Invoked Daily Digests) ## Goal Automatically deliver daily digests to users who opted in, using the existing digest queue. Implementation should be cron-ready: one API route that can be invoked by a scheduler. --- ## Part A — New Cron Endpoint Add: - POST /api/admin/notifications/digest/run RBAC: - Admin/CMO only OR require a shared secret header (preferred) for machine invocation. Use env var: - DIGEST_CRON_SECRET Require header: - X-Cron-Secret: <secret> Return 403 if missing/incorrect. --- ## Part B — Selection Logic For each user with `notification_user_prefs.digestOptIn === true`: - gather queued digest items from `notification_digest_queue` within last 24h - if none, skip - create one digest notification (type `digest.daily`) - delete queued items included Batching: - process users in batches (e.g., 100 at a time) - add safety max per run (e.g., 2000 users) - include `dryRun=true` option: - returns how many users would get a digest without writing Idempotency: - ensure you don't send multiple digests per user per day: - store a record in `digest_runs`: `{ userId, dateKey: 'YYYY-MM-DD', sentAt, countItems }` - before sending, check if exists; if exists, skip unless `force=true` --- ## Part C — Local Time Delivery (MVP approximation) Use global settings hourLocal (from notification_settings.global.digest.hourLocal). Because users may have different timezones later, for MVP: - run cron at the configured hour in server time OR use Africa/Casablanca as default. Document this assumption. Later we can add per-user timezone. --- ## Part D — Observability Emit events: - perf.digestRun with timings and counts - notifications.digest.sent per user (optional, might be noisy) At minimum log summary: - usersProcessed, digestsSent, totalItems --- ## Part E — UI No UI required for v1. Optional (nice): show last digest sent time on profile if available. --- ## Part F — Tests Add script: - scripts/test-digest-cron.ts Seed: - 2 users opted-in with queued items - ensure cron endpoint sends exactly one digest each - second run same day does nothing (idempotent) - dryRun works Add npm script: - test:digest-cron Include in test:api runner. --- ## Deliverables - cron endpoint with secret auth + dryRun + force - per-user idempotency tracking (digest_runs) - batch processing + safety limits - tests - docs update explaining how to schedule the cron call