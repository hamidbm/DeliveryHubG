# Next: Sprint Execution Integration v1 (Capacity + Burn-Up tied to Milestones) ## Goal Make sprints operational and tie sprint execution to milestones so program management can see: - sprint capacity vs committed work - sprint completion (burn-down/burn-up) - milestone progress by sprint - a simple on-track signal derived from sprint completion rate Keep it MVP and explainable; no charts required initially (tables + chips ok). --- ## Part A — Data Model Normalization Confirm current sprint storage: - sprints collection exists (/api/sprints) - work items link to sprints via `sprintId` or similar - forecasting currently uses `workitems_sprints` (Codex note) Goal: - Standardize on: - `sprints` collection with: id, name, startDate, endDate, status, capacityPoints (optional), bundleId (optional) - work item field: `sprintId?: string` If `workitems_sprints` is a join collection, keep it but add a clear single source of truth and helper functions. Add indexes: - workitems: { sprintId: 1, status: 1 } - sprints: { startDate: 1, endDate: 1, status: 1 } - (optional) { bundleId: 1, status: 1 } --- ## Part B — Sprint Rollups API Add: - GET /api/sprints/rollups?bundleId=...&milestoneId=...&sprintIds=... Return per sprint: ```ts type SprintRollup = { sprintId: string; name: string; startDate: string; endDate: string; status: string; scope: { items: number; open: number; done: number; blockedDerived: number; }; capacity: { targetPoints?: number; // sprint capacityPoints if present committedPoints: number; completedPoints: number; remainingPoints: number; utilization: number | null; isOverCapacity: boolean; }; risks: { highCritical: number; }; } ``` Filters: - if milestoneId provided: include only work items in that milestone - if bundleId provided: include only that bundle’s work items - if no filters: return rollups for active/recent sprints only (cap to N) --- ## Part C — Sprint Planning Surface (Minimal UI) Add a minimal UI surface, ideally under Work Items: Option 1: A new tab/view "Sprints" Option 2: Extend existing /work-items plan view with a sprint section Show: - list of active/recent sprints with rollup chips: - committed vs capacity - done vs remaining - blocked derived - high/critical risks - clicking a sprint opens modal listing scoped work items (like roadmap drilldowns) Do NOT build charts yet; table is fine. --- ## Part D — Milestone + Sprint Linkage In Milestone Planning view: - add an optional selector to view sprint rollups for the milestone: - "Sprint view for this milestone" - show sprints affecting this milestone (work items in milestone grouped by sprint) This helps connect milestone readiness to sprint execution. --- ## Part E — Governance (Optional v1) Do not enforce sprint capacity yet unless trivial. But add warnings: - sprint over capacity - too many unestimated items assigned to sprint (storyPoints missing) --- ## Part F — Tests Add `scripts/test-sprint-rollups.ts`: - seed sprints + work items with sprintId + milestoneIds + blockers/risks - validate rollup math + filters Add npm script and include in test:api. --- ## Deliverables - sprint rollups computation + API route - minimal UI to view sprint rollups - milestone planning linkage to sprint rollups - tests + indexes + docs update