# Next: Sprint Governance v1 (Capacity + Close Gate + Audit/Notifications) ## Goal Make sprint execution governable by adding: - sprint lifecycle statuses with gated transitions - capacity enforcement on sprint assignment (when sprint is ACTIVE/COMMITTED) - sprint close readiness evaluation with override reason - audit events + notifications Mirror milestone governance patterns; keep it smaller. --- ## Part A — Sprint Status Model Confirm existing sprint fields/status in /api/sprints. Extend to include: - DRAFT - ACTIVE - CLOSED - ARCHIVED Rules: - Only Admin/CMO (or a Sprint Manager role if exists) can change sprint status. - ACTIVE indicates sprint is executing; capacity enforcement applies. - CLOSED indicates sprint ended; used for velocity. --- ## Part B — Enforce Capacity on Sprint Assignment When setting `sprintId` on a work item (single PATCH and bulk): - If sprint is ACTIVE: - require storyPoints (or timeEstimateHours fallback if you allow) - reject if would exceed sprint.capacityPoints (if defined) unless allowOverCapacity=true - gate allowOverCapacity via canOverrideCapacity (reuse existing authz) Return errors mirroring milestone: - 409 OVER_CAPACITY with details - 400 MISSING_ESTIMATE - 403 FORBIDDEN_CAPACITY_OVERRIDE Implementation: - enforce in PATCH /api/work-items/[id] when sprintId changes - enforce in bulk updates as well --- ## Part C — Sprint Close Readiness Evaluation Add helper (new file or in sprint governance service): evaluateSprintReadiness(sprintRollup) Rules for ACTIVE -> CLOSED: Block close if: - remainingPoints > 0 (open items) AND no override - blockedDerived > 0 (warn or block; choose block for v1) - highCritical risks > 0 (warn or block; choose block for v1) Override: - Admin/CMO only - requires overrideReason Return: { canClose: boolean, blockers: [...], score: number, band: 'high'|'medium'|'low' } --- ## Part D — Sprint API Enhancements Add/extend: - PATCH /api/sprints/[id] - gated transitions: - DRAFT -> ACTIVE - ACTIVE -> CLOSED (readiness gate) - returns sprint + rollup + readiness on success - returns 409 with readiness on block - emits events: - sprints.sprint.statuschanged - sprints.sprint.override (when override used) If an endpoint already exists, extend it. --- ## Part E — UI Controls (Minimal) In the new Sprints tab/view: - show sprint status chip - show Start/Close buttons for Admin/CMO - on Close blocked: show readiness modal with blockers and override reason input Also reuse the over-capacity override modal pattern when sprint assignment fails (if sprint assignments happen from this UI; if not, show error and link to item details). --- ## Part F — Notifications Add notification types: - sprint.status.changed - sprint.close.blocked - sprint.capacity.override Recipients: - Admin/CMO - bundle owners and watchers for the sprint’s bundle scope (if sprint has bundleId) - sprint watchers if you choose to support watching sprints (optional; skip) --- ## Part G — Tests Add `scripts/test-sprint-governance.ts`: - start sprint (DRAFT -> ACTIVE) RBAC enforced - assign item without storyPoints into ACTIVE sprint -> 400 - exceed capacity -> 409 - close blocked when remainingPoints > 0 -> 409 readiness - override close with reason -> succeeds and event/notification created Add npm script and include in test:api. --- ## Deliverables - sprint status model + gated transitions - capacity enforcement on sprint assignment - sprint close readiness + override reason - events + notifications - UI controls in Sprints view - tests + docs update ``` --- # After Sprint Governance v1 At that point you’ll have consistent governance at: - milestone level (strategic) - sprint level (tactical) Then the next natural step is: ## Burn-up/Burn-down Visualization v1 (Optional) A simple milestone-by-sprint burn-up table or chart (if you want visuals), but governance first.