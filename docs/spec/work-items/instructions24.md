# Next: Burn-Up v1 (Milestone Progress by Sprint) + Forecast Calibration ## Goal Provide a simple, accurate milestone burn-up view by sprint, and use it to calibrate Forecasting v1 signals. No complex charting required; start with a table and optional simple chart later. --- ## Part A — Burn-Up API Add endpoint: - GET /api/milestones/[id]/burnup Query params: - bundleId (optional) - sprintLimit (default 12) Response shape: ```ts type MilestoneBurnupResponse = { milestoneId: string; metric: 'points'; // v1 sprints: Array<{ sprintId: string; name: string; startDate: string; endDate: string; status: string; // DRAFT/ACTIVE/CLOSED completedPoints: number; // points completed within this sprint for milestone-scoped items cumulativeCompletedPoints: number; remainingPoints: number; // milestone remaining after this sprint }>; scope: { committedPoints: number; completedPoints: number; remainingPoints: number; }; trend: { avgCompletedPerSprint: number; last3Avg: number | null; acceleration: 'improving'|'flat'|'worsening'|'unknown'; }; }; ``` Definitions: - For each sprint, completedPoints = sum(storyPoints) of milestone-scoped items that moved to DONE in that sprint window. - If you have a work-item history/snapshots, use it; if not, approximate using: - item.status == DONE and item.updatedAt within sprint window (or completedAt if you store it). Prefer a stored `completedAt` field if present; if not, add it on status transition to DONE going forward. --- ## Part B — Data Plumbing: completedAt If work items do not have `completedAt`: - Add `completedAt?: string` in `src/types.ts` - When work item status transitions to DONE (existing /api/work-items/[id]/status or PATCH logic): - set completedAt = now if not already set - If status changes from DONE back to open: - clear completedAt (or keep and store completedAtHistory; v1 can clear) This improves burn-up correctness and forecasting calibration. --- ## Part C — UI: Milestone Planning Burn-Up Panel In `WorkItemsMilestonePlanningView.tsx`: - Add a “Burn-up” panel when a milestone is selected: - render a simple table of last N sprints: - sprint name - completed points - cumulative completed - remaining - Show trend chips: - last3Avg vs avg - acceleration label Do not add charts unless trivial; table is acceptable. --- ## Part D — Forecast Calibration (Lightweight) Update forecasting display (chips) to include a calibration hint: - If last3Avg < avgVelocity * 0.8 => show “velocity down” warning - If last3Avg > avgVelocity * 1.2 => show “velocity up” hint This is display-only; do not change core forecast math yet. --- ## Part E — Tests Add: - scripts/test-burnup.ts Seed: - milestone with committed items - sprints with closed status - items with completedAt in each sprint window Assert: - completedPoints per sprint correct - cumulative/remaining correct - trend computed Add npm script: test:burnup Include in test:api. --- ## Deliverables - /api/milestones/[id]/burnup endpoint - completedAt field set on DONE transition - burn-up panel in milestone planning view - calibration hints in forecast chips (optional but recommended) - tests + docs update (brief)