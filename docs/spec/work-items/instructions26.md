# Next: Jira Integration v1 (One-Way Sync + Mapping + Manual Run) ## Goal Add a safe, one-way Jira sync that can: - import Jira issues as DeliveryHub work items - update existing work items from Jira (title, status, story points, assignee if available) - maintain a stable mapping between Jira issue keys and work items - be manually invoked (no background scheduler yet) Do NOT attempt two-way sync in v1. --- ## Part A — Data Model Extend WorkItem with Jira linkage (if not already present): ```ts jira?: { host: string; // base URL identifier key: string; // e.g. ABC-123 issueId?: string; // Jira internal id url?: string; lastSyncedAt?: string; } ``` Indexes: - { "jira.host": 1, "jira.key": 1 } unique (sparse) --- ## Part B — Admin Configuration Add admin settings storage: - `jira_settings` collection (or reuse admin settings collection) ```ts { _id: "default", host: string, projectKeys: string[], auth: { type: "APITOKEN", email: string, apiToken: string }, // store encrypted if you have a pattern; else env vars for v1 fieldMapping: { storyPointsFieldId?: string, // Jira custom field id statusMapping: Record<string, WorkItemStatus> } } ``` For MVP, prefer env vars if you don’t have encryption: - JIRA_HOST - JIRA_EMAIL - JIRA_API_TOKEN - JIRA_PROJECT_KEYS --- ## Part C — Jira Client (Service) Create: - src/services/jira.ts Implement: - search issues by JQL for configured projects - fetch issue fields needed: - key, summary, status, assignee, updated, url, story points field - rate limit and handle pagination --- ## Part D — Sync API (Admin-only) Add endpoints: 1) Manual sync run - POST /api/admin/integrations/jira/sync body: { projectKey?: string, jql?: string, mode?: "IMPORT_ONLY" | "UPSERT", limit?: number } Behavior: - fetch issues - for each issue: - if exists by jira.host+jira.key, update fields - else create work item (type default STORY or TASK, configurable) - do not overwrite portfolio fields: - bundleId, milestoneIds, dependencies, risk metadata (keep local) - write event: - integrations.jira.sync.completed with counts Response: ```ts { fetched, created, updated, skipped, errors: [...] } ``` 2) Preview - GET /api/admin/integrations/jira/preview?projectKey=...&limit=... Returns sample issues and mapped work item fields without writing. RBAC: - Admin/CMO only Secret handling: - if using env vars, require they exist; return clear error if missing. --- ## Part E — UI (Admin) Add Admin → Integrations → Jira page: - shows config status (connected/missing env) - preview button (shows top 20) - run sync button with: - project dropdown - mode select - limit - shows last run summary (from events) Keep it minimal; no continuous sync yet. --- ## Part F — Work Item UI Enhancements Where a work item has jira linkage: - show Jira key + link on WorkItem details card - allow filtering work items by Jira key/project (optional) --- ## Part G — Tests Add script: - scripts/test-jira-mapping.ts Mock Jira client responses (do not call Jira in tests): - verify import creates items - verify upsert updates title/status/storyPoints but does not overwrite milestoneIds/bundleId - verify unique mapping prevents duplicates Add npm script: - test:jira Include in test:api runner (using mocked jira service). --- ## Deliverables - jira mapping fields on work items + index - jira client service - admin sync + preview endpoints - admin integrations UI page - work item details shows Jira linkage - tests with mocked Jira responses - docs update: how to configure env vars and run sync