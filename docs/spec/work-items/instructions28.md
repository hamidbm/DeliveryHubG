# Next: Work Items UI v2 Hardening + Operability v1 ## Goal Make the new milestone execution board and planning views production-grade: - persistent filters and expanded/collapsed state - deep links to a specific milestone/section - consistent loading/error UX - quick workflows (edit estimate, add dependency, resolve blocker) - measure and prevent performance regressions No new backend engines unless required for these UX improvements. --- ## Part A — State persistence + deep linking ### A1. Roadmap view URL state In WorkItemsRoadmapView: - Persist in the URL query params: - selected bundleId / applicationId filters (if applicable) - expanded milestoneIds (comma-separated) - active milestone tab/section (optional) - On load, read query params and restore state. ### A2. Shareable links Add "Copy link" action on each Milestone Intelligence Header: - copies URL with that milestone expanded + focused - optional fragment: `#milestone=<id>` Acceptance: - refreshing the page keeps the same expanded milestones and filters. --- ## Part B — Loading, error, and empty states Implement consistent UX patterns: - Skeleton/placeholder rows for: - roadmap-intel initial load - milestone expansion (burn-up + sprint rollups) - Error handling: - inline non-blocking errors for per-milestone fetch failures - retry button per milestone - Empty states: - milestone with no items shows "No scoped work items" + link to Milestone Planning --- ## Part C — Action workflows from the board (high value) ### C1. Inline storyPoints edit On WorkItemCard: - allow editing storyPoints inline (small popover) - validate non-negative (already enforced server-side) - update via existing work item PATCH - optimistic UI update with rollback on failure ### C2. Quick dependency add (BLOCKS) From a WorkItemCard or dependency hover: - "Add blocker" / "Blocks…" quick action - opens a small search modal (reuse existing work-item lookup endpoint if available; else add minimal `/api/work-items/lookup?q=` usage) - creates link using `/api/work-items/:id/links` (canonical model) - refresh linkSummary for affected items (local state update or refetch milestone intel lists) ### C3. Resolve blockers quick path For blocked items list modal: - show top blockers with status - provide action: - "Open blocker" (existing) - "Mark blocker DONE" (if user has permission) using status endpoint This should respect RBAC (403 handled cleanly). --- ## Part D — Performance safeguards ### D1. Client-side caching Cache per-milestone expansion fetches: - burnup results cached by milestoneId for the session - sprint rollups cached by (milestoneId, active sprint ids) Invalidate cache on: - work item status change - scope approval decision - sprint close ### D2. Request coalescing If user expands multiple milestones quickly: - avoid duplicate in-flight requests for same milestone - cancel or ignore stale requests when filters change ### D3. Instrumentation Emit client-side timing logs (console only is fine) for: - roadmap initial load time - per-milestone expansion load time Optionally write a perf event if you have a safe client->server perf endpoint; otherwise skip. --- ## Part E — Tests Add lightweight tests (no UI framework required): - extend existing API regression tests to cover: - storyPoints inline update path (API already tested; add one more contract case if needed) - link creation via links endpoint for BLOCKS and inverse derivation correctness - manual QA checklist in docs/wiki/Modules-WorkItems.md: - deep link restore - expand milestone lazy load - add dependency - edit points - resolve blocker - scope request flow still works --- ## Deliverables - Roadmap URL state + shareable milestone links - consistent loading/error/empty states - inline storyPoints edit - quick add dependency flow (BLOCKS) - resolve blocker quick action - caching + request coalescing - updated QA checklist