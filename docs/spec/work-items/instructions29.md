# Next: Data Completeness + Quality Gates v1 (Planning Hygiene) ## Goal Improve planning data quality so forecasting/readiness/burn-up remain reliable: - compute a simple "data quality score" per milestone and sprint - enforce minimal gates in governance transitions - provide "Fix now" workflows (bulk edit) from warnings Do not add complex rules; keep it transparent and actionable. --- ## Part A — Data Quality Scoring (Rollups) Extend: - MilestoneRollup - SprintRollup Add: ```ts dataQuality: { score: number; // 0..100 issues: Array<{ key: string; count: number; detail: string }>; } ``` Compute from scoped open items: Milestone quality checks (open items only): - missingStoryPoints count - missingAssignee count (if assignee exists) - missingDueAt count - missingRiskSeverity count (type=RISK) - missingSprintId count for IN_PROGRESS items (optional) Sprint quality checks: - missingStoryPoints - missingAssignee - missingDueAt - items still TODO/IN_PROGRESS when closing sprint (already readiness) Score example (simple): - start 100 - subtract min(30, missingStoryPoints * 5) - subtract min(20, missingAssignee * 2) - subtract min(20, missingDueAt * 2) - subtract min(30, missingRiskSeverity * 10) Return issues list sorted by impact. Expose counts even when includeLists=false. --- ## Part B — Governance Gates (Minimal) ### Milestone transitions For COMMITTED -> IN_PROGRESS and IN_PROGRESS -> DONE: - if dataQuality.score < 70: - do not hard-block; warn in readiness modal (v1) - if dataQuality.score < 50: - block unless overrideReason provided (Admin/CMO only) Integrate into existing evaluateMilestoneReadiness output as additional blockers. ### Sprint close For ACTIVE -> CLOSED: - if sprint dataQuality.score < 70: - warn - if < 50: - block unless overrideReason Keep this minimal to avoid frustration. --- ## Part C — "Fix Now" Workflows (UI) Where quality issues are shown: - Milestone Planning summary strip - Roadmap milestone header (optional small indicator) - Sprints view rollup chips Add "Fix now" actions for top issues: 1) Missing storyPoints: - open modal listing affected items - allow bulk set storyPoints (single value) or open item editor 2) Missing dueAt: - bulk set due date or open editor 3) Missing risk severity: - bulk set severity for risks (RBAC gated by canEditRiskSeverity) Implementation: - Add a new bulk-edit modal component (reusable) - Use existing PATCH /api/work-items/bulk - Respect RBAC errors and show clear messages. --- ## Part D — API Support If bulk endpoint doesn't support fields: - storyPoints - dueAt - risk severity extend it safely (server validation already exists). Add filter queries for listing "items missing X" per milestone/sprint: - Prefer to reuse existing /api/work-items with filters: - milestoneId + missingField=true If missing, add: - GET /api/work-items/quality?milestoneId=...&issue=missingStoryPoints Keep endpoints minimal. --- ## Part E — Tests Add: - scripts/test-data-quality.ts Coverage: - rollup score + issue counts correct - gates warn/block at thresholds - bulk fix updates items and rollup score improves Add npm script: - test:data-quality Include in test:api runner. --- ## Deliverables - dataQuality score in milestone + sprint rollups - readiness integrates quality thresholds (warn/block) - "Fix now" bulk edit flows for top issues - tests + docs update  

--- ## Optional small tweak (you can decide) Codex asked whether to enable inline storyPoints edit on Roadmap cards too. That’s a nice UX improvement, but it’s not as urgent as data-quality gates. If you do want it, it can be bundled into the “Fix now” workflow work. --- ## Next after Data Quality v1 Once hygiene is enforced, the next big “enterprise wow” feature is: - **Critical Path v1** (dependency graph critical path per milestone, top blockers, and what to do next) But hygiene first, otherwise critical path and forecasts are polluted by bad data.