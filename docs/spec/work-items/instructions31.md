# Next: Cross-Scope Critical Path v1 (Milestone + External Blockers) ## Goal Extend Critical Path so milestone critical chains can traverse external blockers: - blockers in other milestones - blockers in other bundles and produce actionable ownership + navigation. Keep v1 bounded: - only traverse external nodes up to depth N (default 3) - only follow BLOCKS edges (no RELATES_TO) - only include OPEN blockers (DONE contributes 0) --- ## Part A — Extend Critical Path Computation Update `computeMilestoneCriticalPath` to accept: ```ts { includeExternal: boolean; maxExternalDepth: number; } ``` Logic: 1) Start with in-milestone nodes. 2) For each node, include its open blockers even if outside milestone. 3) Expand external blockers recursively up to depth maxExternalDepth. 4) Build a single DAG across all included nodes. Cycle handling: - if cycle detected anywhere, return cycleDetected + cycleNodes. - still compute best-effort path within acyclic subgraph if possible. Estimate handling: - use storyPoints / hours fallback as before. Return additions: ```ts external: { includedNodes: number; depthUsed: number; } nodesByScope: { inMilestone: number; external: number; } ``` And enrich nodes with ownership metadata: - bundleId - milestoneIds - assignee - watchers count (optional) - source scope classification: 'IN_MILESTONE'|'EXTERNAL' --- ## Part B — API Updates Update GET /api/milestones/[id]/critical-path: - support params: - includeExternal=true|false (default false to keep cheap) - maxExternalDepth=3 - limit=100 Return the enriched response. Add server-side caching: - key should include includeExternal + depth + limit - TTL 30s is fine --- ## Part C — UI Enhancements ### Milestone Planning In the Critical Path panel: - add a toggle “Include external blockers” - when enabled: - show external nodes inline but visually labeled: - “External (Bundle X, Milestone Y)” - add quick nav buttons: - “Open bundle” - “Open milestone” - “Open Program view filtered” ### Roadmap In milestone header: - show external blocker count in critical path chip when includeExternal enabled - clicking critical path chip opens panel/modal with the chain including external nodes Keep default off; users can turn it on per milestone. --- ## Part D — Notifications (Optional, small) When includeExternal is enabled and the critical path contains external blockers: - provide a “Notify blocker owners” action that: - sends notifications to bundle owners + watchers of those blocker bundles - type: dependency.criticalpath.externalblocker This can be a button in the UI that calls an admin-only API or uses existing notifications service. If this is too much, skip in v1. --- ## Part E — Tests Extend scripts/test-critical-path.ts to cover: - an external blocker chain of depth 2 - verify it is included when includeExternal=true and excluded otherwise - verify ownership metadata present on external nodes - verify limiting/depth bounding works --- ## Deliverables - critical path supports external traversal (bounded depth) - API params exposed and cached - UI toggle + external labeling + navigation links - tests updated - docs updated with "Cross-scope" mode