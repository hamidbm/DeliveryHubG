# Next: Critical Path Actions v1 (One-click remediation from the critical chain) ## Goal Make the critical path panel actionable by adding workflows for the top recurring remediation tasks: - request estimate - set estimate - assign owner - create dependency or fix wrong dependency - notify owners/watchers (escalation) - propose scope reduction via scope request (optional) Use existing APIs and governance. Add minimal new endpoints only if unavoidable. --- ## Part A — Action Model (Front-end) In critical path response, topActions already exist. Extend actions to include: - ASSIGN - SET_ESTIMATE (admin/bundle owner) - REQUEST_ESTIMATE (notify assignee/owner) - NOTIFY_OWNER (for external blockers) - CREATE_SCOPE_REQUEST (for scope reduction suggestion) Each action should include enough context: - itemId, key, title, bundleId, milestoneIds, reason If the server already emits these, keep; otherwise extend `criticalPath.ts` heuristics. --- ## Part B — Implement Actions in Milestone Planning Critical Path Panel In `WorkItemsMilestonePlanningView.tsx`, for each topAction: ### B1. ASSIGN - If work item has no assignee: - show an assignee picker (reuse existing user search API) - PATCH the work item with assignee - update local state ### B2. SET_ESTIMATE - Inline edit storyPoints for the item (reuse the inline points editor component) - PATCH /api/work-items/:id - On success, refetch critical path panel (or update state) ### B3. REQUEST_ESTIMATE - Sends a notification to: - assignee (if exists) - bundle owners/watchers - Type: workitem.estimate.requested - Body includes link to item + why it is critical path. Implementation options: 1) Add a small endpoint: POST /api/work-items/:id/actions/request-estimate - Server resolves recipients via watchers/bundle owners and calls notifications service. 2) If you prefer no new endpoints, implement as a "create notification" helper behind an admin route. Preferred: dedicated action endpoint for auditability. ### B4. NOTIFY_OWNER (external blocker escalation) - Button: “Notify blocker owners” - Sends notification to blocker bundle owners + watchers - Type: dependency.criticalpath.escalation - Include milestone + blocked item + blocker item details ### B5. CREATE_SCOPE_REQUEST (optional) - For scope reduction suggestion: - open scope request modal prefilled with candidate items (non-critical, low value) - uses existing scope request API If too large, skip in v1. RBAC: - honor existing policies - show disabled buttons with tooltip-style explanation (or inline message) when user lacks permissions - handle 403 gracefully --- ## Part C — Action Feedback + Audit For each action taken: - show confirmation toast/message - emit event: - criticalpath.action.executed with action type, actor, milestoneId, itemId For REQUEST_ESTIMATE and NOTIFY_OWNER: - store these as events and notifications so audit console can explain. --- ## Part D — Roadmap Integration (Light) In Roadmap critical path modal: - allow executing NOTIFY_OWNER for external blockers - allow opening item and setting estimate (if permitted) Do not implement full assigner here; keep major actions in planning view. --- ## Part E — Tests Add scripts/test-critical-path-actions.ts to cover: - request-estimate endpoint creates correct notifications (mock recipients) - notify-owner creates notifications for watchers/owners - RBAC enforced (non-admin cannot set estimate if policy forbids, etc.) Add npm script: test:critical-path-actions Include in test:api. --- ## Deliverables - actionable buttons in critical path panel - request-estimate + notify-owner server endpoints (if needed) - audit events for actions - tests + docs update (Critical Path Actions)