# Next: Policy-as-Config v1 (Configurable thresholds for governance & intelligence) ## Goal Move key thresholds and gating rules from hard-coded constants into admin-configurable policy documents: - readiness gates - data quality gates/scoring weights - forecast bands - critical path slack threshold - sprint close readiness thresholds - optional strictness toggles Keep it v1: - global policy only (no per-bundle overrides yet) - sensible defaults matching current behavior - admin UI to edit and validate - policies are versioned/audited --- ## Part A — Policy Data Model Add collection: `delivery_policies` Doc: ```ts type DeliveryPolicy = { _id: 'global'; version: number; updatedAt: string; updatedBy: string; readiness: { milestone: { warnScoreBelow: number; // default 70 blockScoreBelow: number; // default 50 blockOnBlockedItems: boolean; blockOnHighCriticalRisks: boolean; }; sprint: { warnScoreBelow: number; // default 70 blockScoreBelow: number; // default 50 blockOnBlockedItems: boolean; blockOnHighCriticalRisks: boolean; }; }; dataQuality: { weights: { missingStoryPoints: number; // default 5 missingAssignee: number; // default 2 missingDueAt: number; // default 2 missingRiskSeverity: number; // default 10 }; caps: { missingStoryPoints: number; // default 30 missingAssignee: number; // default 20 missingDueAt: number; // default 20 missingRiskSeverity: number; // default 30 }; }; forecasting: { atRiskPct: number; // default 0.15 offTrackPct: number; // default 0.30 minSampleSize: number; // default 3 }; criticalPath: { nearCriticalSlackPct: number; // default 0.10 defaultIncludeExternal: boolean; // default false defaultExternalDepth: number; // default 3 }; }; ``` --- ## Part B — Policy Loader + Caching Create: - src/services/policy.ts Functions: - getDeliveryPolicy(db): returns global policy, inserts defaults if missing - validateDeliveryPolicy(policy): ensures ranges and types (0-100 etc) - cache in-memory with short TTL (e.g. 30s) --- ## Part C — Wire Policy into Computation Replace hard-coded values in: - milestoneGovernance.ts (readiness warn/block thresholds) - sprintGovernance.ts (readiness thresholds) - db.ts (data quality scoring weights/caps) - forecasting.ts (bands thresholds, min sample size) - criticalPath.ts (near-critical slack threshold, external default/depth) Ensure behavior is identical with default policy. --- ## Part D — Admin UI + API Add Admin page: - Admin → Settings → Delivery Policy API: - GET /api/admin/delivery-policy - PUT /api/admin/delivery-policy - increments version - writes updatedAt/updatedBy - emits event: policy.updated with diff summary UI: - form with sections + input validation - "Restore defaults" button - show current version and last updated info RBAC: - Admin/CMO only --- ## Part E — Tests Add: - scripts/test-policy.ts Coverage: - defaults inserted and used - changing thresholds affects readiness block behavior - changing data quality weights changes score - policy validation rejects invalid values Add npm script: test:policy and include in test:api. --- ## Deliverables - delivery policy model + loader + cache - computation wired to policy instead of constants - admin API + UI to edit policy - audit event on updates - tests + docs update (Admin policy section)