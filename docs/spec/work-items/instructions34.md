# Next: Visualization Upgrade v1 (Critical Path Graph + Dependency Explorer) ## Goal Add a dependency graph visualization for a milestone that: - renders BLOCKS edges as a DAG (best-effort) - highlights critical path nodes and edges - supports external blockers when enabled - allows click-through to work item details - provides filtering (critical only / full graph / depth limit) Keep it minimal and performant. No new engine logic required beyond light API support if needed. --- ## Part A — Graph Data API (Reuse existing if possible) Prefer reusing: - GET /api/milestones/[id]/critical-path?includeExternal=...&maxExternalDepth=... If needed, extend response to include: - edges: Array<{ fromId: string; toId: string }> - nodes: Array<{ id; key; title; status; bundleId; milestoneIds; remainingPoints; scope; isCritical; isNearCritical }> If edges are not included today, add them to the critical-path API response to avoid extra fetches. --- ## Part B — UI Component: DependencyGraph Add component: - src/components/DependencyGraph.tsx Implementation constraints: - use a lightweight library if already present; otherwise implement a simple SVG-based DAG layout: - layer nodes by topological depth - horizontal lanes for branches - support: - zoom/pan (optional, but nice) - node click: open work item - hover: show key metrics (points, blocked count, risks) - highlight critical path edges/nodes Graph modes: - "Critical only" - "Critical + near-critical" - "Full (bounded)" with a max node limit (e.g., 150) If cycleDetected: - show a clear banner and fall back to a simplified list view. --- ## Part C — Integrate into Milestone Planning In the Milestone Planning view: - Add a tab or expandable section under Critical Path: - "Graph" - Toggle includeExternal and depth from existing controls - Provide mode selector (critical/full) --- ## Part D — Integrate into Roadmap (Light) In Roadmap critical path modal: - add "View graph" button that opens the milestone planning graph section (or a modal graph) --- ## Part E — Performance - fetch graph data only when graph is opened - cache per milestone (respect policy version and includeExternal settings) - cap nodes to avoid slow renders --- ## Part F — Tests No UI tests required. Add API contract test: - critical-path API returns edges and nodes fields when requested. Update docs/wiki/Modules-WorkItems.md with a short section on graph view. --- ## Deliverables - dependency graph component - critical-path API includes nodes+edges for rendering - graph integrated in milestone planning and linked from roadmap - caching + node caps - docs update