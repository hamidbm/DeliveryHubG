# Next: Policy Inheritance v1 (Global + Bundle Overrides) ## Goal Allow bundle-specific overrides for delivery policy thresholds, inheriting from the global policy: - global policy remains the default - bundles can override selected fields - computations use the effective policy for each bundle/milestone/sprint Keep it v1: - overrides at bundle level only - no milestone-level overrides yet - UI for admins to edit bundle overrides - full audit trail via events --- ## Part A — Data Model Add collection: - delivery_policy_overrides Schema: ```ts type DeliveryPolicyOverride = { _id: string; // bundleId (simplest) or generated id bundleId: string; version: number; updatedAt: string; updatedBy: string; overrides: Partial<DeliveryPolicy>; // only fields present here override }; ``` Index: - unique { bundleId: 1 } --- ## Part B — Policy Resolver Extend `src/services/policy.ts` with: - getEffectivePolicyForBundle(bundleId) Logic: 1) load global policy 2) load bundle override (if any) 3) deep-merge: effective = merge(global, overrides) 4) return effective + a `policyRef`: ```ts { effective, refs: { globalVersion, bundleVersion?: number }, hasOverrides: boolean } ``` Cache: - cache effective policy per bundleId for 30s (like global) Validation: - validate override doc after merge (use same validateDeliveryPolicy) - allow partial docs but validate merged result --- ## Part C — Wire into Computations Where policy is used, switch from getDeliveryPolicy() to “effective policy”: Milestones: - determine bundle context: - If milestone is bundle-scoped (has bundleId), use that bundle’s effective policy - If milestone spans multiple bundles: - v1 rule: use the strictest effective policy among visible bundles in scope: - warnScoreBelow = max(...) - blockScoreBelow = max(...) - dataQuality weights/caps: take max weights/caps - forecasting thresholds: strictest (smaller tolerance) - criticalPath slack: smallest slack - include in response: policyRefs per contributing bundle and resolved strategy ("strictest") Sprints: - if sprint has bundleId, use bundle policy - else use global Critical path: - use policy from milestone scope strategy above - include policyRefs in response Expose in rollups: - add `policy: { strategy, globalVersion, bundleVersions? }` to milestone/sprint rollups for transparency. --- ## Part D — Admin API + UI ### API - GET /api/admin/bundles/[bundleId]/delivery-policy - PUT /api/admin/bundles/[bundleId]/delivery-policy RBAC: Admin/CMO only. ### UI In Admin: - Add “Bundle Policy Overrides” section: - list bundles with indicator “Has overrides” - click bundle -> policy editor - editor shows: - effective values - override values (editable) - “Reset to global” button On save: - bump override version - emit event: policy.bundle.updated with diff summary --- ## Part E — Tests Add scripts/test-policy-overrides.ts: - create global policy - create bundle override (e.g., blockScoreBelow=80) - verify milestone rollup uses bundle policy - verify multi-bundle milestone resolves strictest - verify cache invalidation after update - verify RBAC on admin endpoints Add npm script: test:policy-overrides and include in test:api. --- ## Deliverables - bundle override storage + resolver - computations use effective policy - transparent policy refs in rollups - admin UI to edit overrides - tests + docs update (Admin + Policy section)