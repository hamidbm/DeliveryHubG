# Next: Policy Overrides Completion v1 (make overrides actually affect computations) From your summary, I see: - override list API + admin UI - critical-path cache key fix - tests + docs What’s still required (unless already done but not mentioned) is end-to-end wiring so overrides change behavior in rollups/governance. Please implement/confirm the following: ## Part A — Data + APIs (single bundle override) 1) Persist overrides in a dedicated collection (delivery_policy_overrides) keyed by bundleId with version/updatedAt/updatedBy. 2) Add admin endpoints: - GET /api/admin/bundles/[bundleId]/delivery-policy - PUT /api/admin/bundles/[bundleId]/delivery-policy (or equivalent endpoints) so UI isn’t writing through a list endpoint. RBAC: Admin/CMO only. Emit event: policy.bundle.updated with diff summary. ## Part B — Policy resolver in src/services/policy.ts Add: - getEffectivePolicyForBundle(bundleId) - getEffectivePolicyForMilestone(milestoneId) with multi-bundle strategy: - strictest across bundles in scope (v1) Return: - effective policy - policyRefs { globalVersion, bundleVersions[] } - strategy string for milestones ("global"|"bundle"|"strictest") Cache per bundle and per milestone (TTL 30s). ## Part C — Wire effective policy into computations Replace current global policy usage in: - milestoneGovernance.ts readiness thresholds + gating toggles - sprintGovernance.ts thresholds + gating toggles - db.ts dataQuality weights/caps in rollups - forecasting.ts bands + minSampleSize - criticalPath.ts slack + default external settings Rules: - if item/milestone/sprint is bundle-scoped -> use that bundle effective policy - if milestone spans bundles -> strictest effective policy across included bundles ## Part D — Transparency in API responses Include in: - MilestoneRollup - SprintRollup - CriticalPathResult a policy reference block: ```ts policy: { strategy: 'global'|'bundle'|'strictest', globalVersion: number, bundleVersions?: Array<{ bundleId: string; version: number }> } ``` This makes behavior explainable. ## Part E — Tests Extend scripts/test-policy-overrides.ts to assert: - readiness warn/block thresholds change when override set - dataQuality score changes when override weights change - forecasting minSampleSize and bands change - multi-bundle milestone uses strictest policy - policy refs appear in rollups ## Deliverables - effective policy resolver + computation wiring - proper admin CRUD for overrides - rollups expose policy refs - expanded tests