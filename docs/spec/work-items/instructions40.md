# Next: Event Taxonomy + Alias Layer v1 (stabilize event naming without breaking history) ## Goal Stabilize event naming and filtering by introducing: - a canonical taxonomy for event types - a backward-compatible alias mapping layer - normalized category/prefix fields for queries and UI filtering We must NOT lose historical events or require data migration. --- ## Part A — Define Canonical Event Types (Docs + Code) Create a single source of truth: - docs/wiki/Operations.md (or Admin.md) section: "Event taxonomy" - src/services/eventsTaxonomy.ts Include: - canonical type strings - category mapping (governance, scope, dependency, criticalpath, integrations, notifications, security, perf) - alias mapping: - e.g. map: - workitem.github.linked -> workitem.github.pr.linked - workitem.github.merged -> workitem.github.pr.merged - (and any other known drift) Export functions: - normalizeEventType(rawType): { canonicalType, category, modulePrefix } - getEventCategory(type) --- ## Part B — Normalize at Read Time (No migration) Update event readers: - /api/feed - /api/admin/events (audit) - any other event listing endpoint So that responses include: ```ts { type: rawType, canonicalType, category, modulePrefix } ``` UI should filter on canonicalType/category, but still display original type where useful. --- ## Part C — Update Event Emit Path (Optional, forward-only) Keep emitting whatever the code emits today. But add a helper in emitEvent: - store both: - type (raw) - canonicalType - category - modulePrefix for new events going forward If changing stored schema is risky, skip and just normalize on read. --- ## Part D — Update Change Feed Transformations ChangeFeed mapping should: - match on canonicalType (preferred) - fall back to rawType for unknowns Ensure GitHub events show as: - "PR linked to work item" - "PR merged" --- ## Part E — Tests Add: - scripts/test-event-taxonomy.ts Assertions: - normalization maps github linked/merged to canonical pr.* types - feed transforms based on canonical types - audit listing includes category/modulePrefix fields Add npm script: test:event-taxonomy and include in test:api. --- ## Deliverables - eventsTaxonomy module with alias + category mapping - feed + audit APIs return canonicalType/category - ChangeFeed uses canonicalType - tests + docs update