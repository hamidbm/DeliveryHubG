# Next: Ownership Model v1 (RACI-lite) + Auto-Assignment Suggestions ## Goal Add lightweight accountability primitives so program execution is scalable: - milestone owner (accountable) - work item owner (responsible) is already assignee; strengthen it - owner gaps surfaced prominently and fixable via “Fix now” - auto-assignment suggestions for critical-path items and blockers Keep it minimal and aligned with existing users/bundle assignments/watchers. --- ## Part A — Data model ### A1. Milestone owner Extend Milestone with: ```ts ownerUserId?: string; ownerEmail?: string; ``` (Prefer userId; store email for display fallback.) ### A2. Work item ownership consistency Assignee exists; ensure: - assigneeUserId (or existing field) is normalized - add `assignedAt?: string` when assignee set (optional but useful) --- ## Part B — Ownership suggestions service Add: - src/services/ownership.ts Functions: 1) suggestOwnersForWorkItem(workItemId) - candidates: - bundle owners (bundle_assignments) - bundle watchers (notification_watchers on bundle) - last N assignees for same bundle/type (history heuristic) - rank: - bundle owner highest - recent assignee next - watchers next Return: ```ts { candidates: Array<{ userId; email; reason; score }> } ``` 2) suggestOwnersForMilestone(milestoneId) - if milestone single-bundle: suggest bundle owner(s) - multi-bundle: suggest Admin/CMO + top bundle owners by scoped points Return same shape. --- ## Part C — APIs ### C1. Set milestone owner - PATCH /api/milestones/[id] accept { ownerUserId } (and/or ownerEmail) RBAC: - Admin/CMO or bundle owner(s) for bundles in scope Emit events: - milestones.milestone.ownerchanged Notify: - new owner - milestone watchers Type: - milestone.owner.changed ### C2. Owner suggestions endpoints - GET /api/work-items/[id]/owner-suggestions - GET /api/milestones/[id]/owner-suggestions RBAC: - must be able to view the item/milestone (visibility rules) --- ## Part D — UI integration ### D1. Milestone Planning header - Show milestone owner chip: - if missing: warn indicator + "Set owner" button - Clicking opens picker (reuse user search) - Optionally show suggested owners. ### D2. Critical Path Actions For ASSIGN actions: - if item has no assignee: - show suggested owners list first - one-click assign to top suggestion ### D3. Data Quality integration Extend data quality issues (optional but valuable): - missingAssignee already exists; upgrade "Fix now" to support bulk assign in milestone/sprint views: - bulk assign selected owner to selected items --- ## Part E — Tests Add: - scripts/test-ownership.ts Coverage: - suggestions include bundle owners/watchers - set milestone owner emits event + notification - RBAC enforced - visibility enforced Add npm script: test:ownership and include in test:api. --- ## Deliverables - milestone owner fields + API support - ownership suggestion service + endpoints - milestone planning UI owner control - critical-path assign uses suggestions - optional bulk assign fix-now - tests + docs update