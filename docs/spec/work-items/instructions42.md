# Next: Staleness Signals v1 (Aging + Nudges + Digest integration) ## Goal Detect and surface stale work across: - critical path items - blocked items - unassigned items - items with no updates - stale PR activity (GitHub) Then: - surface in Roadmap/Milestone Planning as a chip + list - include in daily digest and notifications - add "nudge" actions to ping owners/watchers Keep it v1: simple thresholds, policy-configurable later. --- ## Part A — Staleness definitions (v1 defaults) Define stale thresholds: - work item stale if: - status not DONE and updatedAt older than 7 days - critical stale if: - item is critical path and updatedAt older than 3 days - blocked stale if: - isBlocked and updatedAt older than 5 days - unassigned stale if: - no assignee and createdAt older than 2 days - GitHub stale if: - has linked open PR and PR.updatedAt older than 5 days - OR no linked PR and item is IN_PROGRESS older than 5 days Add these thresholds to DeliveryPolicy later; for v1 can be constants. --- ## Part B — Server-side computation Extend: - Milestone rollups and sprint rollups to include: ```ts staleness: { staleCount: number; criticalStaleCount: number; blockedStaleCount: number; unassignedStaleCount: number; githubStaleCount: number; } ``` Add endpoint for list drilldown: - GET /api/work-items/stale?milestoneId=...&kind=critical|blocked|unassigned|github|all - Also support sprintId and bundleId Return list of items with: - key, title, status, assignee, updatedAt, daysStale, reason, github activity summary Ensure visibility filtering applies. --- ## Part C — UI surfaces ### C1. Roadmap milestone headers Add a “Stale” chip showing: - total stale - critical stale emphasized Click opens modal list (reuse existing modal patterns). ### C2. Milestone Planning summary strip Add stale chip + "View stale items" quick link. ### C3. Critical Path panel For each critical node show: - days since update - activity chip already exists; incorporate into stale scoring --- ## Part D — Nudges (notifications) Add a nudge action in stale list modal: - “Nudge owner” Behavior: - sends notification to: - assignee (if set) - milestone owner (if set) - bundle owners/watchers Type: - workitem.stale.nudge Body: - why it is stale + links RBAC: - any viewer can nudge (or restrict to watchers/owners); choose: watchers/owners/admin to reduce spam. Also include stale summary in daily digest (if digest enabled): - add digest-eligible type workitem.stale.summary (or embed into digest builder). --- ## Part E — Tests Add scripts/test-staleness.ts: - seed items with different updatedAt/createdAt, critical path status, blockers, github PR timestamps - verify rollup staleness counts - verify stale list endpoint returns correct items and reasons - verify nudge creates notification and event Add npm script: - test:staleness Include in test:api. --- ## Deliverables - staleness counts in rollups - /api/work-items/stale drilldown endpoint - roadmap + planning UI chips and drilldowns - nudge action with notifications + events - digest inclusion (minimal) - tests + docs update