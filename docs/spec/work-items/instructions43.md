# Next: Staleness Policy + Bundle Overrides v1 (Configurable thresholds + spam control) ## Goal Move staleness thresholds and nudge controls into DeliveryPolicy (global + bundle overrides), and ensure behavior uses effective policy: - milestone/sprint rollups - stale drilldown endpoint - nudge action permissions and rate limits - digest inclusion thresholds This makes staleness operationally safe and adaptable. --- ## Part A — Extend DeliveryPolicy schema Add to DeliveryPolicy (global, overrideable): ```ts staleness: { thresholdsDays: { workItemStale: number; // default 7 criticalStale: number; // default 3 blockedStale: number; // default 5 unassignedStale: number; // default 2 githubStale: number; // default 5 inProgressNoPrStale: number; // default 5 }; nudges: { enabled: boolean; // default true allowedRoles: Array<'ADMIN'|'CMO'|'BUNDLE_OWNER'|'WATCHER'>; // default ['ADMIN','CMO','BUNDLE_OWNER','WATCHER'] cooldownHoursPerItem: number; // default 24 (prevent spam) maxNudgesPerUserPerDay: number; // default 20 }; digest: { includeStaleSummary: boolean; // default true minCriticalStaleToInclude: number; // default 1 }; } ``` Update validation + normalization accordingly. --- ## Part B — Policy-aware staleness computation Update staleness logic: - src/lib/staleness.ts should accept a policy object rather than constants: - computeStalenessFlags(item, context, policy) - rollups should use effective policy: - milestones: use getEffectivePolicyForMilestone (already exists) - sprints: bundle policy if sprint has bundleId, else global Update /api/work-items/stale: - determine effective policy based on scope (milestone/sprint/bundle) - use those thresholds for filtering and reasons. Expose policy refs in stale response: ```ts policy: { strategy, globalVersion, bundleVersions } ``` --- ## Part C — Nudge spam controls Add collection: - staleness_nudges Schema: ```ts { workItemId: string, nudgedBy: string, nudgedAt: string, reason: string, } ``` Indexes: - { workItemId: 1, nudgedAt: -1 } - { nudgedBy: 1, nudgedAt: -1 } Enforce: - cooldownHoursPerItem: do not allow nudging same item too frequently - maxNudgesPerUserPerDay: cap per user RBAC: - only allow if user role matches allowedRoles in effective policy - also require visibility (already enforced) If blocked: - return 429 or 409 with clear error code: - NUDGE_COOLDOWN - NUDGE_LIMIT_REACHED - NUDGE_DISABLED - NUDGE_FORBIDDEN Emit event: - workitem.stale.nudge.blocked with reason (optional) --- ## Part D — Admin UI In Admin Delivery Policy editor: - add a Staleness section to edit thresholds and nudge/digest settings. Because overrides exist, bundle overrides can also tweak these. --- ## Part E — Tests Add scripts/test-staleness-policy.ts: - verify thresholds change stale classification - verify bundle override affects classification for that bundle/milestone - verify nudge cooldown and per-user cap - verify RBAC driven by policy allowedRoles Add npm script: - test:staleness-policy Include in test:api. --- ## Deliverables - policy schema extended + UI editor supports it - staleness computation uses effective policy everywhere - nudge rate limiting + cooldown with persistence - tests + docs update (Operations + Admin)