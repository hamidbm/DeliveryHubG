# Next: Monte Carlo Forecasting v1 (P50/P80/P90 milestone ETA + hit probability) ## Goal Augment forecasting from a single-point ETA to a distribution: - P50/P80/P90 finish dates - probability of hitting milestone endDate - use velocity sample distribution + remaining points uncertainty - surface in rollups, roadmap, and capacity planning Keep it v1: - weekly bucket simulation - based on historical throughput distribution - deterministic seed for tests --- ## Part A — Policy config Extend DeliveryPolicy.forecasting with: ```ts monteCarlo: { enabled: boolean; // default false (safe rollout) iterations: number; // default 2000 useCriticalPath: boolean; // default true minSampleSize: number; // reuse existing or override pLevels: number[]; // default [0.5, 0.8, 0.9] } ``` Make this overrideable by bundle. --- ## Part B — Simulation engine Add: - src/services/monteCarlo.ts Inputs: - remainingPoints (prefer critical path remaining if useCriticalPath and available, else milestone remaining) - velocity samples per week (array from existing velocity calc) - optional modifiers: - penalty for high staleness / blocked / critical risks (v1 simple multiplier) - milestone endDate Algorithm: - For each iteration: - sample weekly throughput from empirical distribution of historical weekly throughput - accumulate until remainingPoints <= 0 - record finish date (week bucket end) - Compute: - p50/p80/p90 dates - probability finish <= endDate - mean and std dev (optional) Return: ```ts type MonteCarloForecast = { enabled: boolean; iterations: number; remainingPointsUsed: number; p50: string; p80: string; p90: string; hitProbability: number; // 0..1 }; ``` Determinism for tests: - accept seed parameter or use a seeded RNG when process.env.NODE_ENV === 'test'. --- ## Part C — Integrate into rollups and forecasting Extend MilestoneRollup.forecast to include monteCarlo: - forecast.monteCarlo?: MonteCarloForecast Compute only if: - policy.forecasting.monteCarlo.enabled - velocity.sampleSize >= minSampleSize - remainingPoints > 0 Expose in: - roadmap intel - program intel (aggregated milestone risk based on P80 slip) - capacity plan recommendedActions: if P80 > endDate, recommend SLIP_MILESTONE or SCOPE_REDUCE. --- ## Part D — UI surfaces ### D1. Milestone headers (Roadmap + Planning) Show: - P80 date and hit probability chip Example: - "P80: May 21 (42%)" ### D2. Capacity tab For each driver milestone, show: - P80 date vs endDate - highlight if miss probability high Add tooltip/hover for P50/P80/P90. --- ## Part E — Tests Add: - scripts/test-monte-carlo.ts Seed: - deterministic velocity samples - known remaining points - fixed seed Assert: - p50/p80/p90 ordering and stable values - hit probability computed correctly Include in test:api. --- ## Deliverables - monteCarlo service with seeded RNG for tests - policy config + admin UI support - rollups include monteCarlo forecast when enabled - roadmap/planning/capacity surfaces show P80 + probability - tests + docs update (Forecasting section)