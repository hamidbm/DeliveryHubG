## What’s next Codex’s summary mentions **UI + tests + docs**, but it does **not** explicitly confirm the core backend deliverables were implemented: - `decision_log` collection + indexes creation - `/api/decisions` list/create endpoint - `/api/decisions/[id]` read endpoint - visibility + RBAC enforcement in those APIs - auto-create decision entries from override paths (commit/readiness/capacity/scope approvals) - weekly brief integration pulling decisions via API/service Some of that must exist for the UI/tests to pass, but given the summary wording, the safest next step is a **backend completeness / correctness check** and to ensure decisions are consistently generated from all override paths (not just commit override). So the next thing to do is: ## Next: Decision Log Backend Completion + Override Coverage v1 This is a short hardening step to ensure the decision log is real, consistent, and audit-grade. --- # Tell Codex next (copy/paste) ```markdown # Next: Decision Log Backend Completion + Override Coverage v1 Your summary mentions Decisions UI, tests, and docs. Please confirm/implement the backend pieces explicitly and ensure override coverage is complete. ## Part A — Ensure Decision Log APIs exist and are correct Verify or implement: 1) POST /api/decisions - validates required fields: scopeType, title, rationale, outcome, decisionType - enforces RBAC: - PROGRAM: Admin/CMO only - BUNDLE/MILESTONE/WORK_ITEM: Admin/CMO or bundle owner for the bundle(s) in scope - enforces visibility (private bundles) - writes decision_log entry with createdAt/by 2) GET /api/decisions?scopeType=...&scopeId=...&limit=...&cursor=... - cursor pagination stable by createdAt+_id - visibility enforced; returns only visible decisions 3) GET /api/decisions/[id] - visibility enforced Indexes: - { scopeType:1, scopeId:1, createdAt:-1 } - { 'related.milestoneId':1, createdAt:-1 } - { decisionType:1, createdAt:-1 } ## Part B — Auto-create decisions from ALL override/approval paths Today tests mention commit override; expand coverage and ensure entries are created consistently for: 1) Commit review override - decisionType: COMMIT_OVERRIDE - rationale: overrideReason - related: milestoneId, commitReviewId, policyRef 2) Readiness override (start/done) - decisionType: READINESS_OVERRIDE - related: milestoneId - rationale: overrideReason 3) Capacity override (allowOverCapacity) - decisionType: CAPACITY_OVERRIDE - related: milestoneId (if available), bundleId, workItemIds - rationale: overrideReason OR "allowOverCapacity" with details (if reason missing, add UI/API to require reason when overriding) 4) Scope requests approve/reject/cancel - decisionType: SCOPE_APPROVAL - related: scopeRequestId, milestoneId, workItemIds if known - rationale: decision rationale if provided; otherwise require in decide API (recommended) or store minimal. 5) Risk accepted (optional v1) - if you have any explicit “risk accept” workflow later, leave stub. Emit event: decisions.created for auto-created entries as well. ## Part C — Weekly brief integration correctness Ensure weekly brief generation includes decisions created within the week: - for milestone brief: decisions where scopeType=MILESTONE and scopeId=milestoneId - for bundle brief: scopeType=BUNDLE and scopeId=bundleId - for program brief: scopeType=PROGRAM Also include high-severity decisions linked to milestone via related.milestoneId even if scopeType differs (optional). ## Part D — UI guardrails - In Milestone decisions create form: - enforce rationale required - allow setting severity + tags - Show when an entry was auto-created (flag like source='AUTO'|'MANUAL'). If source flag is not present, add: ```ts source: 'AUTO'|'MANUAL' ``` ## Part E — Tests Extend scripts/test-decision-log.ts: - readiness override creates decision - capacity override creates decision - scope approval creates decision (approve and reject) - weekly brief includes at least one AUTO decision Include in test:api. ## Deliverables - explicit decision APIs + indexes - full override path coverage for auto-created decisions - weekly brief includes decisions reliably - updated tests ``` --- ## Why this is next Decision Log is only valuable if it’s: - consistently populated (especially for overrides) - reliably queryable - visibility-correct This hardening step ensures it’s audit-grade, not a UI-only feature.