## What’s next Decision Log is now audit-grade and closes the human-governance loop. At this point, the product is functionally complete for a real org. The next “big win” is to make it **operationally observable and self-tuning** across all the engines you’ve built (rollups, intel, drift, briefs, digests, GitHub sync, capacity plan). Right now you emit some perf events, but you don’t have a unified way to answer: - Which endpoints are slow and why? - Which scheduled jobs are drifting in runtime or failing? - Are we hitting cache correctly? - Are we sending too many notifications? - Are we generating too many nudges/briefs? So the next step is: ## Next: SLO + Performance Dashboard v1 (Job health + API latency + cache hit rates) This uses your existing `events` and audit consoles, but makes it actionable and non-admins don’t need raw JSON. --- # Tell Codex to implement next (copy/paste) ```markdown # Next: SLO + Performance Dashboard v1 (Ops dashboard for jobs + APIs + cache) ## Goal Create a lightweight operational dashboard to monitor: - scheduled jobs health (digest run, drift scheduler, weekly brief, GitHub sync manual runs) - API performance (p50/p95) for key endpoints - cache effectiveness (hit/miss) for expensive computations - notification volume trends No external observability stack required. Use existing events collection + perf.* events. --- ## Part A — Standardize perf event payloads (forward-only) Ensure these events emit consistent fields: - perf.commitdrift.run - perf.digestRun - perf.roadmapIntel - perf.milestoneRollups - perf.commitDriftRun (if used) - integrations.github.sync.completed (treat as perf source) Standard payload shape: ```ts { name: string, at: string, durationMs: number, ok: boolean, scope?: { bundleId?: string; milestoneId?: string }, counts?: Record<string, number>, cache?: { hits: number; misses: number }, error?: { code?: string; message?: string } } ``` If current payloads differ, normalize at emit time for new events. --- ## Part B — Metrics aggregation API Add: - GET /api/admin/ops/metrics?windowDays=7 RBAC: Admin/CMO only. Returns: - jobs: lastRunAt, lastDurationMs, lastOk, failuresLast7d, avgDurationMs - apis: per endpoint name, p50/p95 duration, call count, error rate - cache: hit rate per cache name (criticalPath, policy, milestoneRollups, drift, briefs) - notifications: counts per type per day (last 7 days) Implementation: - query events collection for relevant perf/counter events - group by type + day - compute percentiles (approx ok; sort durations) - cap query window and results for performance --- ## Part C — Admin UI: Ops Dashboard Add Admin → Operations → Ops Dashboard: - Job health table - API latency table - Cache hit-rate table - Notifications volume table - Filter by window (1d/7d/30d) Each row should link to Admin Audit Events filtered by that event type/time. --- ## Part D — Lightweight instrumentation improvements Add per-request timing in key endpoints if not already: - /api/work-items/roadmap-intel - /api/program/intel - /api/milestones/rollups - /api/capacity/plan Emit perf events with endpoint name and duration. Add cache hit/miss counters in caches: - policy cache - milestone policy cache - criticalPath cache - drift snapshots fetch Emit aggregated counts in perf event payload. --- ## Part E — Tests Add scripts/test-ops-metrics.ts: - seed events with known durations and validate: - percentiles computed correctly - job summary correct - notification counts grouped by day/type Add npm script: - test:ops-metrics Include in test:api. --- ## Part F — Docs Update docs/wiki/Operations.md: - Ops dashboard usage - what the metrics mean - troubleshooting steps --- ## Deliverables - standardized perf payloads (forward-only) - /api/admin/ops/metrics aggregation endpoint - Admin Ops Dashboard UI - instrumentation additions for key endpoints - tests + docs ``` --- ## Why this is next You’ve created multiple “mini engines” with schedulers and caches. An ops dashboard is what prevents regressions as the system grows, and it gives you real evidence about where to optimize next.