## What’s next You’ve now built the core product, the executive layer, the control plane, and ops observability. At this point, the highest-leverage next step is not another feature; it’s reducing the time-to-value for new users and making the system “self-explaining” in a way that drives adoption. Right now, a new user still has to learn a lot: - what to look at first - how to interpret confidence, drift, P80, capacity, data quality - which actions matter most So the next step should be: ## Next: Guided Onboarding v1 (Role-based “Start Here” + embedded explanations + action checklist) This is a productization step that will make everything you built actually usable for non-experts. --- # Tell Codex to implement next (copy/paste) ```markdown # Next: Guided Onboarding v1 (Role-based Start Here + embedded explanations) ## Goal Add a lightweight onboarding and guidance layer that: - helps first-time users understand what to do - explains key intelligence metrics in-context - provides a role-based checklist (PM vs Engineer vs Exec/Admin) - links directly to the relevant action surfaces (fix data quality, unblock critical path, run commit review, etc.) Keep it v1: - no LLM required - all content is deterministic, stored as JSON - track completion per user --- ## Part A — Onboarding data model Add collection: - user_onboarding Schema: ```ts { userId: string, role: 'PM'|'ENGINEER'|'EXEC'|'ADMIN', createdAt: string, updatedAt: string, completedSteps: string[], // step ids dismissedTips: string[], // tip ids } ``` Index: - unique { userId: 1 } --- ## Part B — Onboarding content registry Add: - src/services/onboardingContent.ts Defines: - roles and default role inference: - Admin/CMO -> ADMIN - bundle owner -> PM - else ENGINEER (v1) - checklist steps: - "Set your digest preferences" - "Watch a bundle you care about" - "Open Program page and review at-risk bundles" - "Fix data quality for a milestone" - "Run commit review for a draft milestone" - "Resolve a critical path action" Each step includes: - title - why it matters - CTA link (href) and optional action name Also defines "tips" for key metrics: - P80/hit probability - Drift - Data Quality score - Critical Path - Overcommit calendar - Staleness --- ## Part C — APIs Add: - GET /api/user/onboarding - PATCH /api/user/onboarding body: { role?, completeStepId?, dismissTipId?, reset?: boolean } --- ## Part D — UI surfaces ### D1. Home / Dashboard Add a "Start Here" card: - shows role selector - shows checklist with progress bar - each step has CTA button - allow dismiss ### D2. Embedded metric tooltips Add small "What is this?" helper popovers on: - P80/hit probability chips - Drift chip - Data Quality score - Capacity overcommit cells - Critical path chip Tooltips should pull from onboardingContent tips. ### D3. Program page Add a "How to use this page" collapsible help section for first-time visitors. --- ## Part E — Tests Add scripts/test-onboarding.ts: - onboarding doc created on first GET - role inference works - PATCH updates completion/dismissed - visibility: any auth user only edits self Add npm script: test:onboarding and include in test:api. --- ## Part F — Docs Update docs/wiki/Home.md: - explain onboarding card - explain roles and checklists --- ## Deliverables - onboarding persistence + APIs - content registry - Start Here UI card - metric tooltips tied to content registry - tests + docs ``` --- ## Why this is next You’ve built a very capable system. Onboarding and embedded explanations are what convert capability into adoption, especially for stakeholders who didn’t help build it.