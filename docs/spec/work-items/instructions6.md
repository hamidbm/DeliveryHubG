# Next: Release Readiness v1 (Milestone Readiness Gates + Status Transitions) ## Goal Add a lightweight but enterprise-credible release readiness system to milestones: - readiness checklist (entry/exit criteria) - readiness evaluation derived from rollups (blocked, risks, overdue, capacity, remaining work) - gated milestone status transitions (COMMITTED -> IN_PROGRESS -> DONE) - RBAC + override with auditing This should build on existing rollups and governance (do not redesign roadmap yet). --- ## Part A — Milestone Status Transitions in UI (Complete Lifecycle) In Milestone Planning view: 1. Add controls: - COMMITTED -> IN_PROGRESS - IN_PROGRESS -> DONE 2. RBAC: - Only Admin/CMO can transition milestone statuses 3. Use PATCH /api/milestones/[id] with `{ status: 'IN_PROGRESS' }` or `{ status: 'DONE' }` 4. After transition, refetch milestones + rollups. Keep controls compact (similar to Commit button). --- ## Part B — Readiness Rules (Derived, No New DB Yet) Implement readiness evaluation purely from existing rollup output. Create in `src/services/milestoneGovernance.ts` (or new `milestoneReadiness.ts`): ### `evaluateMilestoneReadiness(rollup: MilestoneRollup)` Return: ```ts type MilestoneReadiness = { canStart: boolean; // for COMMITTED -> IN_PROGRESS canComplete: boolean; // for IN_PROGRESS -> DONE blockers: Array<{ key: string; detail: string; severity: 'info'|'warn'|'block' }>; score: number; // 0..100 band: 'high'|'medium'|'low'; } ``` #### Start Gate (COMMITTED -> IN_PROGRESS) default rules Block transition if any are true: - targetCapacity exists AND rollup.capacity.isOverCapacity === true (unless override) - rollup.capacity.remainingPoints > 0 AND committedPoints == 0 (meaning no scoped work) (warn only) - rollup.totals.blockedDerived > 0 (warn only for start; do not block start) - high/critical risks exist (warn only for start) #### Complete Gate (IN_PROGRESS -> DONE) default rules Block transition if any are true: - rollup.capacity.remainingPoints > 0 OR rollup.capacity.remainingHours > 0 - rollup.totals.blockedDerived > 0 - (high + critical) open risks > 0 - overdueOpen > 0 Optionally block if: - isLate == true AND remaining > 0 (already covered) Score: - Similar to confidence scoring but focused on “exit readiness” - Start at 100, subtract for remaining work, blockers, high risks, overdue, overcapacity. Band thresholds same: >=80 high, >=60 medium else low. --- ## Part C — Gated PATCH /api/milestones/[id] Update the milestone PATCH handler to enforce gates: 1. When transitioning: - COMMITTED -> IN_PROGRESS: call readiness evaluation, block if `canStart=false` unless `{ allowOverride: true }` - IN_PROGRESS -> DONE: block if `canComplete=false` unless `{ allowOverride: true }` 2. Override rules: - override allowed only for Admin/CMO (already RBAC) - require a reason string when overriding: - request body includes: `{ status: 'DONE', allowOverride: true, overrideReason: '...' }` - if allowOverride is true and overrideReason missing, return 400. 3. Response: Return milestone + rollup + readiness evaluation summary (so UI can display why it was blocked). --- ## Part D — UI Readiness Feedback When user attempts status transition: - If blocked (409), show a modal listing readiness blockers with details. - If override is allowed (Admin/CMO), show textarea for overrideReason and a confirm button. - On success, refetch milestones + rollups. This mirrors the over-capacity override UX you already built. --- ## Part E — Audit Events Log events on: - milestone status transitions - overrides (include overrideReason and readiness blockers snapshot) Use existing events mechanism (or add minimal insert to `events` collection if already present). Event payload should include: - milestoneId - oldStatus -> newStatus - actor - allowOverride - overrideReason - readiness snapshot (keys + details + score) --- ## Part F — Tests Add tests similar to milestone governance tests: - Non-admin cannot transition statuses. - COMMITTED -> IN_PROGRESS allowed without override under normal conditions. - IN_PROGRESS -> DONE blocked when remainingPoints > 0. - IN_PROGRESS -> DONE blocked when open high/critical risks > 0. - Override allowed with allowOverride + overrideReason, and event is written. --- ## Deliverables - UI controls for IN_PROGRESS and DONE transitions - Readiness evaluation function - Server-side gated transitions + override reason - Readiness feedback modal - Audit events - Tests + package.json script `test:milestone-readiness` ``` --- # After Release Readiness v1 Once readiness exists, the next step is: ## Roadmap Intelligence v1 (Now non-cosmetic) - show milestone readiness/confidence in roadmap lanes - cross-bundle blocker signals (blocker in another milestone/bundle) - “top blockers” + “top risks” summary per milestone - roadmap confidence becomes computed, not decorative At that point, enhancing the Work Items roadmap is finally worth doing. --- # What You Should Run Now Before moving on, run: ```bash npm run build npm run test:milestone-governance