# Next: Work Items Roadmap Intelligence v1 (Non-Cosmetic) ## Goal Upgrade the Work Items Roadmap view from “timeline viewer with bubbles” into an enterprise delivery intelligence surface by integrating: - milestone rollups (capacity/confidence/readiness) - dependency signals (cross-milestone blockers, critical blockers) - risk propagation signals (per milestone, high/critical emphasis) - actionable drilldowns (click to see blockers/risks/overdue lists) Do not rebuild the whole module; iterate on existing Roadmap view. --- ## Part A — Roadmap Data Contract (API) Roadmap UI needs one consolidated endpoint that returns: - milestones in scope - rollup + readiness for each milestone - key “problem lists” per milestone (top blockers, blocked items, high/critical risks, overdue) ### Implement endpoint Add: - `GET /api/work-items/roadmap-intel` Query params: - `bundleId` (optional) - `applicationId` (optional) - `milestoneIds` (optional) - `includeLists=true|false` (default true) Response shape: ```ts type RoadmapIntelResponse = { milestones: Array<{ milestone: Milestone; rollup: MilestoneRollup; readiness: MilestoneReadiness; lists?: { topBlockers: Array<{ id; key; title; status; blocksOpenCount }>; blockedItems: Array<{ id; key; title; status; openBlockersCount }>; highRisks: Array<{ id; key; title; severity; status }>; overdueOpen: Array<{ id; key; title; dueAt; status }>; crossMilestoneBlocks: Array<{ blockedId; blockedKey; blockerId; blockerKey; blockerMilestoneId; blockedMilestoneId; }>; }; }>; }; ``` ### Implementation notes (db.ts) Build on existing functions: - milestone rollups already exist - readiness evaluation already exists - dependency graph already exists Add helper functions: - `getTopBlockersByMilestone(milestoneId, limit)` - blockers are items with outgoing BLOCKS edges; count open dependents in the same milestone - `getBlockedItemsByMilestone(milestoneId, limit)` - items where derived isBlocked true, include openBlockersCount - `getHighRisksByMilestone(milestoneId, limit)` - type==RISK, severity in {high, critical}, status != DONE - `getOverdueOpenByMilestone(milestoneId, limit)` - `getCrossMilestoneBlockingEdges(milestoneIds)` - find BLOCKS edges where blocker milestoneId != blocked milestoneId Keep limits small (e.g., 10) to avoid heavy payloads. --- ## Part B — Roadmap UI Enhancements (Target: src/components/WorkItemsRoadmapView.tsx) ### B1. Milestone header intelligence row For each milestone lane/header, show: - Confidence score/band - Readiness band (canStart/canComplete status depending on milestone status) - Capacity utilization chip - Blocked derived count - High+critical risks count - Late slipDays (if late) Use compact chips similar to Milestone Planning view. ### B2. Cross-milestone dependency highlights Add a small indicator when a milestone has cross-milestone blockers: - “X cross-milestone blocks” Click opens a modal listing edges and provides navigation to blocker/blocked items. ### B3. Drilldown modals (Actionable) Add “View blockers”, “View blocked”, “View risks”, “View overdue” links per milestone header. Each opens a lightweight modal listing items (key/title/status) with click-through to item details. ### B4. No major redesign Keep existing timeline layout. The intelligence is additive. --- ## Part C — Performance and Caching - Roadmap view should fetch intel once per scope change. - Use `includeLists=false` when just rendering overview and fetch lists on demand (optional optimization). - Ensure DB queries use indexes (milestoneIds, status, links.targetId). --- ## Part D — Tests (API-level) Add a test script (similar style) that: - seeds a few milestones and work items with blockers and risks - calls /api/work-items/roadmap-intel - validates: - readiness included - crossMilestoneBlocks detects edges across milestones - topBlockers list contains expected items --- ## Deliverables - /api/work-items/roadmap-intel endpoint - Roadmap view shows milestone intelligence row - Drilldown modals for blockers/blocked/risks/overdue - Cross-milestone dependency visibility - API test script + npm script ``` --- # Why This Is the Right Next Step This is the first time enhancing the roadmap will actually matter, because: - milestones now have enforcement and readiness (not cosmetic) - dependencies are canonical and queryable - rollups are real signals - you can compute cross-bundle/milestone coordination and confidence --- # What You Should Run Now Before handing Codex the next task, run: ```bash npm run build npm run test:milestone-governance npm run test:milestone-readiness ``` If those pass, proceed with the Roadmap Intelligence v1 task above.



